import { HighlightcursorDirective } from './highlightcursor.directive';

describe('HighlightcursorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightcursorDirective();
    expect(directive).toBeTruthy();
  });
});
